# Rectangle web service on Railway
# Recommended Railway service root directory: apps/web

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# The runtime image is nginx; its own CMD serves the built app.
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
