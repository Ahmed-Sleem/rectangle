# Rectangle Docker Compose example values.
# Copy to .env locally or configure equivalent secrets in your deployment platform.
# Never commit real secrets.

POSTGRES_DB=rectangle
POSTGRES_USER=rectangle
POSTGRES_PASSWORD=replace-with-a-strong-database-password
SESSION_JWT_SECRET=replace-with-at-least-32-random-characters
CORS_ORIGIN=http://localhost:3000
WEB_PORT=3000
API_PORT=8080
