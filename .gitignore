# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*.code-workspace

# Env & secrets
.env
.env.*
!.env.example
*.pem
*.key
*credentials*
.gh-token

# Dependencies / build (if code is added later)
node_modules/
.venv/
__pycache__/
dist/
build/
.next/
coverage/
.turbo/

# Internal / local only (never publish)
_working_docs/
uploads/
edgepilot-ai/
*.local.md
thinking/

# App
apps/web/dist/
apps/*/node_modules/
*.tsbuildinfo
.vite/
coverage/

# End-to-end run output. Traces and screenshots are diagnosis for one run and
# can contain a rendered page; they are never a repository artefact.
e2e/test-results/
e2e/playwright-report/
